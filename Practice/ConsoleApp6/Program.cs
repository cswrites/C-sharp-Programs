using System;
class Program
{
    static void Main()
    {
        string numberString = "123";
        int number = int.Parse(numberString);
        Console.WriteLine("Parse meaning converting a data froom one type to another");
        Console.WriteLine("Parsed Number: " + number);
        Console.ReadLine();  //Can use Clt+f5 instead
    }
}
